# Copy this file to .env and fill it in before running `docker compose up`
# anywhere other than your own laptop. .env is gitignored — never commit
# real secrets.
#
# Generate a strong value with, e.g.:
#   openssl rand -base64 24

POSTGRES_PASSWORD=
